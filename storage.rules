rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for portfolio images/website assets
      allow read: if true;
      
      // Only authenticated users can upload/modify files
      allow write: if request.auth != null;
    }
  }
}
